import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';
// import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
	// <React.StrictMode>
	// 	<App />
	// </React.StrictMode>,
	<App />, //51ms49ss,
);

//useState, using props in React Hook, reuse components, useEffect, Custom Hook, race condition, recursion in hook, useEffect with/without return fn,
